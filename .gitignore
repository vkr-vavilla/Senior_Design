# --- Python ---
__pycache__/
*.pyc
*.pyo
*.pyd
*.egg-info/
.venv/
venv/
backend/venv/
backend/.venv/

# --- Node / Next.js ---
node_modules/
.next/
frontend/.next/
frontend/node_modules/
.npm/
.pnp.*

# --- Env files (secrets) ---
.env
.env.local
.env*.local
.env.production
.env.development
backend/.env
frontend/.env.local

# --- Model weights / training artifacts (too large for GitHub) ---
training/artifacts/
training/checkpoints/
training/runs/
# LoRA adapter export dirs dropped at repo root (multi-GB)
qwen2.5-7b-chatml-qlora-v2/
qwen*-qlora*/
*.safetensors
*.bin
*.pt
*.pth
*.onnx
backend/kokoro_models/

# --- Experiment tracking ---
wandb/
mlruns/
runs/

# --- Hugging Face cache ---
.cache/huggingface/

# --- Build outputs ---
dist/
build/
*.log

# --- IDE / OS ---
.vscode/
.idea/
.DS_Store
Thumbs.db
.claude/
frontend/.next/

# Large model artifacts
training/artifacts/
backend/kokoro_models/
wandb/
*.bin
*.onnx
*.safetensors
# GGUF build outputs (multi-GB) and the base-model/toolchain download cache.
# These are produced by scripts/build_gguf.sh and published to a model registry,
# never committed — a single quantized base is ~4.5 GB, well over GitHub's limit.
*.gguf
ollama/models/
.cache/
data/raw/leetcode/
*.swp

# --- Docker ---
.docker/
