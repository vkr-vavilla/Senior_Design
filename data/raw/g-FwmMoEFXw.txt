For today's software engineering interview, design autocomplete for Google search. My top two priorities are fast responses and scale. For scale, we're going to make sure we're using a horizontally scalable design from the beginning using ECS plus application load balancer. For speed, we're going to be doing everything in memory using a try. Can you explain how that try works? A try is an extremely effective data structure if you want to do things like store words or phrases character by character. Each character has a pointer to the next possible characters until you reach a completed word. For example, if somebody types po, we see that we have port and pork as possible things they could be searching for. So, you'll query that data structure for every request. We'll also introduce a cache to store frequent prefixes that get looked up a lot. This is helpful if something's trending and people keep looking up the same prefixes, which is actually very common in real world search. And how will you rank the possibilities you get from the try? Every time a search is completed, we'll send an event to asynchronous pipeline, which can update our try later. Primary ranking factor will be frequency of search.