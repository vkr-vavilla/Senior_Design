For today's software engineering interview, explain how distributed systems can automatically recover from failures. In a distributed system, failures are inevitable. Nodes can crash, requests can be dropped, or databases can lose data. But most large scale systems are capable of automatic recovery. Okay, but how can they actually recover? A simple example is if one of your nodes can no longer serve traffic, a load balancer can simply stop serving traffic to that node. At least now we aren't dropping requests that go here. Can they recover that failed node? Yes, an orchestration system like Kubernetes or ECS can automatically detect when a node fails and restart it, bringing it back online. Then our load balancer will continue serving traffic to that node once it's recovered. What about for a smaller short-term issue? For intermittent failures like a one-off network issue, we can configure our system with retries throughout. Just remember, if you do add retries, always use exponential backoff and have a hard cap on how many retries you can do. Usually not more than two. What if the most important node in the system that's orchestrating things fails? Yes, some systems do have a leader. And if the leader does crash, a different node in the cluster should be able to take over.