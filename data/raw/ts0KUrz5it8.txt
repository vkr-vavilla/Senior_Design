What's the difference between a URL and a domain name? Okay, let's start with a domain name. This is what you actually own and it's just the root identity of your online presence. This is not a website or anything. It's just a name. Okay, that doesn't sound very useful. Well, it's useful because you can use that name to point to other things. For example, a website, an API, or an email server. Now, instead of someone having to memorize the IP of your website, they can just memorize your domain name. And where does all that information actually live? When you buy a domain name, you usually get access to a DNS zone. In practice, this is basically a text file where you can configure different things about your domain. Okay, but then where is that DNS zone? How does the internet find it? This information is kept in a name server, usually something like Cloudflare or Route 53. When somebody wants to get information about your domain, essentially the content of this text file, they need to go to this name server first, which has that content. Okay. Then what is a URL? A URL is everything something needs to get to a specific page or resource on your site. It includes the protocol, the fully qualified domain name, and the path to get a specific resource.