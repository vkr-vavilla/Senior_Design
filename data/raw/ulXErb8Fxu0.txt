For today's software engineering interview, explain how you'd handle authentication for a website. I will just use basic O. On every single request from the client to the server, we'll include this header with a B 64 encoded version of their username and password which the server can validate against our username and password in the database. Okay, so we have to share unencrypted credentials on every single request with this method. Can you do it another way? That's a good point. Instead, let's use session off. A user will make one request with their username and password. we can validate it on the server side and issue them a session key via cookie which is stored in memory on the server side. Any subsequent request from the client will include that session ID which we can validate against what we have in server memory. So they don't have to send their username and password every time. Better but this won't work in a distributed environment unless we're using a shared state with something like Reddus. Okay, in that case we can use JWT O. The user will make a login request and the server will use a secret key to generate a JWT that they can pass back to validate themselves on every single request. And why is this better than session off? This is better because we're not depending on server memory to store a session ID. Any server on the back end which has access to this secret key and the JWT pass from the client can validate that user. So no matter how many servers we have on the back end, if they have this secret key, they can validate that request.