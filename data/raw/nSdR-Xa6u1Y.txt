For today's software engineering interview, explain how you debug a slow database. The first thing I do is collect data. Look at what queries are taking long, how long they're taking, and if this only happens while under load or all the time. Okay, makes sense. Then what would you do next? I'd look at specific requests to make sure they're doing exactly what I think they're doing, and they're doing it the best way possible. It's very easy to write a query that gets data, but does it very inefficiently. All right. And what if the queries look fine? If the queries look good, I'd make sure that our database is set up to handle the most common access patterns as efficiently as possible. We can do things like adding indexes or even adjusting the entire structure of the tables if necessary. Okay, let's say your database is set up well and you already have the indexes in place. If that doesn't help and queries are repeated and computationheavy, we can introduce a cache between the database and the server to store the results of common requests so we can instantly return those. What if requests aren't repeated or caching doesn't help? If none of that works, we might be using the wrong database altogether. For example, trying to use Postgress when the only operation we want is a fuzzy search. It works, but there are better options out there like elastic search. All right, I'll follow for more real world system design advice.