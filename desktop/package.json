{
  "name": "prepai-desktop",
  "version": "0.1.1",
  "private": true,
  "description": "PrepAI desktop app — native shell that runs the local interview stack.",
  "scripts": {
    "tauri": "tauri",
    "dev": "tauri dev",
    "build": "tauri build",
    "icon": "tauri icon"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2"
  }
}
