{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Core permissions for the FinalRound splash/bootstrap window.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
