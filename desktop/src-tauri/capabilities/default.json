{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Core permissions for the PrepAI splash/bootstrap window.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
